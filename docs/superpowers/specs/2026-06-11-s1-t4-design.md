# s1-t4: s1 Test-Time Scaling on Colab T4

> Spec — 2026-06-11
> 基于 s1: Simple test-time scaling (Muennighoff et al., 2025)

## 一、项目目标

在 Colab T4 (15.6GB VRAM) 上验证 s1 论文核心链路：
用官方 s1K 数据集子集，QLoRA 微调 Qwen2.5-7B-Instruct，实现 Budget Forcing，生成 test-time scaling curve。

### 成功标准

1. SFT 后 MATH500 子集 (200题) 提升 ≥ 3 个百分点（vs 基座模型 7B-Instruct）
2. Budget Forcing 产生正向 scaling 斜率（更多 thinking tokens → 更高 accuracy）
3. 产出完整 results：PNGs (loss curve + scaling curve)、logs (train/eval)、metrics (JSON)

### 范围边界

- 做：s1K 数据过滤、QLoRA 训练、BF 实现、MATH500 子集评估、结果收集
- 不做：AIME24/GPQA 评估、多方案对比实验、并行扩展 (majority voting / REBASE)、完整的 s1 论文消融复现

---

## 二、数据流水线

### 加载

```python
from datasets import load_dataset
ds = load_dataset("simplescaling/s1K", token=HF_TOKEN)
```

HF token 读取自 `~/.huggingface/access_token` 或项目根 `.huggingface/access_token`。

### 过滤：1000 → ~300

| 阶段 | 方法 | 预计剩余 |
|------|------|----------|
| 质量 | 检查 `<|im_start|>think` / `<|im_start|>answer` 标记完整性 | 950+ |
| 难度 | 用 Qwen2.5-7B-Instruct 评估（temperature=0），筛掉基座答对的题；保留推理长度 > 中位数 | ~500 |
| 多样性 | 按 MSC 领域分类，每领域最多取 ceiling(n_domain / total * 300)，保证覆盖 | ~300 |

### 格式

保留 s1K 原始格式，loss 只在 reasoning_trace + solution 上计算（mask question tokens）。

```
<|im_start|>user
{question}
<|im_start|>assistant
<|im_start|>think
{reasoning_trace}
<|im_start|>answer
{solution}<|im_end|>
```

### 数据复用

所有过滤后的数据保存为 JSONL 到 `/content/s1-t4/s1k_filtered.jsonl`，下次 session 直接加载。

---

## 三、训练架构

### 模型与量化

- 基座：Qwen2.5-7B-Instruct (bf16)
- 量化：4-bit NF4 (bitsandbytes)
- 微调：QLoRA

### QLoRA 参数

```
r=16, alpha=32, dropout=0.05
target_modules: q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj
约 40M 可训练参数
```

### 训练超参

```
epochs: 3
per_device_batch_size: 2
gradient_accumulation_steps: 8  → effective batch = 16
lr: 2e-4
schedule: cosine, 5% warmup
max_seq_length: 4096
optimizer: paged_adamw_8bit
dtype: bf16
```

预计训练时间：2-3 小时 (T4)。

### Checkpoint

每 50 steps 保存 adapter 权重到 `/content/s1-t4/checkpoints/adapter_step_{n}.pt`。

---

## 四、Budget Forcing

### 实现方式

HuggingFace `model.generate()` + 自定义 `LogitsProcessor`。

不使用 vLLM — vLLM 不支持 token 级别的解码干预。

### 提前结束 (max_thinking_tokens)

```
当 output_tokens >= max_thinking 且仍在 thinking 阶段：
  LogitsProcessor 将 end-of-thinking token (<|im_start|>answer) 置为最高 logit
  → 模型被迫输出答案
```

### 延长推理 (min_thinking_tokens)

```
当模型尝试输出 end-of-thinking token 时：
  压制该 token
  追加 "Wait\n" 到已生成序列
  继续生成 → 模型可能自我纠正
```

### 预算点配置

| 配置 | max_thinking | BF extend |
|------|-------------|-----------|
| baseline (no BF) | none | none |
| BF-1 | 2048 | 1x Wait |
| BF-2 | 4096 | 2x Wait |

### 基线对比

- **基座 + CoT**：Qwen2.5-7B-Instruct 原版，无 SFT，标准 CoT prompt
- **基座 + CoT + BF**：同上，但强制扩展推理 — 验证 BF 在没有 SFT 时是否有效（分离 SFT 和 BF 的效果）

---

## 五、评估

### 基准

MATH500 随机子集 200 题（从 official 500 中固定种子采样，保证可复现）。

### 推理参数

- temperature: 0 (greedy)
- max_new_tokens: 取决于 BF 配置（2048 / 4096 / 8192）
- 所有评估使用相同的随机种子

### 三维度量（s1 论文原版）

- **Control**: thinking tokens 落在预算范围内的比例
- **Scaling**: accuracy-token 曲线的平均斜率
- **Performance**: 该配置下的最高 accuracy

### 每题评估

精确答案匹配：提取 `<|im_start|>answer` 和 `<|im_end|>` 之间的内容，与 ground truth 比较（支持数值容忍度匹配）。

---

## 六、结果收集

### VM 内目录结构

```
/content/s1-t4/
├── checkpoints/
│   └── adapter_step_{n}.pt
├── results/
│   ├── train_loss.png        # loss curve, matplotlib
│   ├── scaling_curve.png     # accuracy vs thinking tokens
│   ├── metrics.json          # {control, scaling, performance} per config
│   └── per_question.jsonl    # 每题是否答对
├── logs/
│   ├── train.log             # 训练日志
│   └── eval.log              # 评估日志
└── s1k_filtered.jsonl        # 过滤后的数据集
```

### 本地拉取方案

用 `CronCreate` 每 10 分钟执行：

```bash
colab download /content/s1-t4/results/ projects/s1-t4/results/
colab download /content/s1-t4/logs/ projects/s1-t4/logs/
```

REST API 走 `colab.pa.googleapis.com`，绕过 WebSocket 不稳定问题。

---

## 七、项目文件

```
projects/s1-t4/
├── train.py                # QLoRA 训练主脚本
├── dataset.py              # s1K 加载、过滤、格式化
├── budget_forcing.py       # LogitsProcessor + BF controller
├── evaluate.py             # 多预算点评估 + metrics 计算
├── charts.py               # loss curve + scaling curve 绘图
├── launch.py               # Colab bootstrap: pip install + spawn train
├── check_progress.py       # 训练监控
└── results/                # 本地拉取的结果目录
```

---

## 八、依赖

```
torch >= 2.0
transformers >= 4.45
datasets
bitsandbytes
peft
accelerate
matplotlib
```

Colab 预装大部分，只需额外 pip install `bitsandbytes` 和 `peft`。

---

## 九、风险

| 风险 | 缓解 |
|------|------|
| 7B 模型推理能力不足，SFT 后未见提升 | 基座 zero-shot 基线先行评估，确认有提升空间 |
| 训练 OOM (T4 15.6GB) | 降 batch_size → 1, grad_accum → 16 |
| BF 扩展导致模型陷入循环 | 循环检测：连续3次 "Wait" 后无新内容则强制结束 |
| Colab WebSocket 断开 | 训练用 detached subprocess；REST API 拉取结果 |
| s1K 样本过长截断损失信息 | 过滤时优先保留 < 4096 tokens 的样本 |

---

*Spec 完成于 2026-06-11*
